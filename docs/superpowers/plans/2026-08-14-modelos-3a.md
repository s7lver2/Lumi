# Subsistema 3a — Gestión de modelos — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** convertir la instalación de pesos —hoy editar cuatro registros JSON a mano, colocar
licencias y reiniciar el daemon— en una pantalla: elegir nivel (Mini/Pro/Vision), aceptar
licencias, descargar y verificar, con progreso visible en toda la app aunque no estés mirando.

**Architecture:** los cuatro registros (`modelos`, `verificadores`, un `motores` nuevo, y un
manifiesto nuevo para `geo`) ganan `fichero_url`/`licencia_url`/`licencia_texto`/`puerta`. La
descarga reutiliza el runner de tareas existente (`crates/lumid/src/tasks.rs`) con un `TaskKind`
nuevo que ejecuta `workers/lumi_bajar.py`. El id de la tarea activa vive en `meta` (como ya vive
`models_dir`), así que cualquier sesión del panel la descubre sin depender de `localStorage`. Los
toasts se montan en `AdminPanel`, no en la vista de Modelos, y sondean `GET /v1/admin/model-task`.

**Tech Stack:** Rust (axum, rusqlite) en `lumid`/`lumi-index`/`lumi-proto`; Python stdlib
(`workers/lumi_bajar.py`); React/TypeScript/Tailwind en `client/`.

## Global Constraints

- **Sin pruebas nuevas salvo lógica pura.** Solo la resolución de qué falta por nivel (contando
  compartidos una vez) y el cálculo de tamaño total van en `cargo test -p lumi-index` — todo lo
  demás (rutas, UI, el script Python) se verifica ejecutando, per `CLAUDE.md`.
- **Un commit por tarea terminada.** Español en todo: código, comentarios, mensajes de commit.
- **`ponytail`**: la simplificación más simple que sirva, con su comentario `// ponytail:` cuando
  aplique.
- **Dark theme, sin verde nunca** («hecho» es blanco), mono para lo que produce una máquina
  (hashes, bytes, timestamps), solo iconos de `client/src/ui/Icon.tsx` — la lista actual es
  `check, pause, spinner, refresh, alert, chevron, signal-off, x, user, clock, device, shield,
  plus, bell, logo, layers, users, search, image, folder, back, trash, globe, logout, expand,
  lock, cloud, boxes`. Ninguna tarea de este plan necesita un icono nuevo: reutiliza `cloud`
  (descarga), `boxes` (modelos), `shield` (licencia/puerta).
- **"En la pantalla no se justifica nada"** — nada de prosa de diseño en la UI real; el porqué
  vive en la spec, no en el producto. El mockup (`docs/superpowers/specs/lumi-s3a-mockups.html`,
  secciones 1–8) ya tiene el texto y las clases exactas a copiar — no inventar redacción nueva.
- **`limits::effective`** sigue siendo la única forma legítima de leer límites por usuario; nada
  en este plan la toca.
- **Nunca «cargar de todas formas».** Un hash que no cuadra borra el fichero; sin excepción, sin
  flag, sin "usar igual" — mismo criterio que Qdrant en el subsistema 1.
- **El `map_key` de `crates/lumid/src/routes/map.rs` se guarda EN CLARO en `meta`, nunca se
  devuelve al cliente** (`has_key: bool` es lo único que sale). El token de proveedor de pesos de
  este plan sigue ese mismo patrón exacto — no se introduce cifrado nuevo para un secreto del
  mismo riesgo que uno que el proyecto ya trata así. `// ponytail:` en el sitio donde se guarda.

---

## Mapa de ficheros

**Rust — crea:**
- `crates/lumid/src/routes/models.rs` — todas las rutas nuevas de este subsistema.
- `workers/lumi_bajar.py` — descarga, verifica, escribe licencia, emite progreso.

**Rust — modifica:**
- `crates/lumi-index/src/registro.rs` — `Modelo`/`Verificador` ganan 4 campos; `Motor` nuevo.
- `crates/lumi-index/src/geo.rs` — `RecursoGeo` nuevo + `cargar_recursos`.
- `crates/lumi-proto/src/api.rs` — `TaskKind::ModelDownload`, tipos de respuesta nuevos.
- `crates/lumid/src/tasks.rs` — nuevo brazo en `command()`, `model_task_id` en spawn/finish.
- `crates/lumid/src/store.rs` — `model_licenses` table, migración para `models_dir` (ya existe).
- `crates/lumid/src/queue/mod.rs` — carga `modelos`/`verificadores`/`motores`/`geo`; `recargar()`.
- `crates/lumid/src/main.rs` — registra las rutas nuevas.
- `crates/lumid/src/routes/map.rs` — nada de código, solo referencia de patrón (no se toca).

**Registros — crea:**
- `registros/motores/qwen3-vl.json`, `paddleocr.json`, `depth-anything-v2-small.json`.
- `registros/geo/registro.json` — manifiesto de `paises`/`koppen` con sus licencias.

**Client — crea:**
- `client/src/admin/ModelosView.tsx` — la pantalla de niveles (mockup §2).
- `client/src/admin/LicenciasGate.tsx` — rejilla + lector + puerta (mockup §3 y §5).
- `client/src/admin/ModelToasts.tsx` — los dos toasts (mockup §6).
- `client/src/wizard/ModelosStep.tsx` — el paso del asistente, envuelve `ModelosView`.

**Client — modifica:**
- `client/src/admin/AdminPanel.tsx` — quita `"modelos"` de `PRONTO`, monta `ModelToasts`.
- `client/src/admin/Hueco.tsx` — quita la entrada `modelos` de `QUE`.
- `client/src/admin/KeysView.tsx` — la fila «Proveedor de pesos» deja de ser un cartel.
- `client/src/lib/api.ts` — interfaces nuevas, `TaskStatus.kind` gana `"model_download"`.
- `client/src/App.tsx` — un paso nuevo del asistente.

---

### Task 1: Los registros ganan sus cuatro campos, y nace el de motores

**Files:**
- Modify: `crates/lumi-index/src/registro.rs`
- Modify: `crates/lumi-index/src/geo.rs`
- Create: `registros/motores/qwen3-vl.json`, `registros/motores/paddleocr.json`,
  `registros/motores/depth-anything-v2-small.json`
- Create: `registros/geo/registro.json`
- Test: `crates/lumi-index/src/registro.rs` (módulo `#[cfg(test)]` al final del fichero)

**Interfaces:**
- Consume: nada.
- Produce: `Modelo`, `Verificador`, `Motor`, `RecursoGeo` con los campos nuevos; `cargar_motores`,
  `RecursoGeo::cargar` — la Tarea 3 (Queue) y la Tarea 5 (endpoint de estado) los consumen.

- [ ] **Step 1: Los cuatro campos en `Modelo` y `Verificador`**

En `crates/lumi-index/src/registro.rs`, añade a `pub struct Modelo` (después de `pesos_url`) y a
`pub struct Verificador` (mismo sitio) estos cuatro campos, todos con `#[serde(default)]` para no
romper los JSON existentes que no los traen:

```rust
    /// La URL directa del peso — no la página del proyecto, que es
    /// `pesos_url` y se conserva. Vacío significa «no se puede bajar solo»:
    /// la pantalla pasa a modo guía en vez de a un botón que fallaría.
    #[serde(default)]
    pub fichero_url: String,
    /// De dónde sale el texto de licencia que se enseña antes de aceptar.
    #[serde(default)]
    pub licencia_url: String,
    /// El texto cacheado, para que la pantalla de aceptación funcione sin
    /// red. Vacío significa «hay que ir a buscarlo» — se resuelve en la
    /// Tarea 2, antes de tocar ningún peso.
    #[serde(default)]
    pub licencia_texto: String,
    /// `None`, o `Some("token")` cuando el proveedor exige credencial propia
    /// (hoy solo RoMa v2, por DINOv3). Ningún otro valor tiene sentido hoy;
    /// se deja como `Option<String>` y no un booleano por si un proveedor
    /// futuro exige algo distinto de un token.
    #[serde(default)]
    pub puerta: Option<String>,
```

- [ ] **Step 2: `Motor`, el registro que hoy no existe**

Los tres motores (Qwen3-VL, PaddleOCR, Depth Anything V2 Small) viven hoy solo como clases Python
en `workers/lumi_motores.py`, con el hash en un `sha256.txt` junto a los pesos y no en un JSON —
mira `_hash_esperado` en ese fichero. El registro nuevo tiene que respetar ese contrato: no puede
llevar `sha256` en el JSON porque Python no lo lee de ahí para motores.

Añade a `crates/lumi-index/src/registro.rs`:

```rust
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct Motor {
    pub id: String,
    pub nombre: String,
    /// `vlm` | `ocr` | `profundidad` — la clave de `CLASES` en
    /// `workers/lumi_motores.py`. Debe existir ahí o el motor no carga.
    pub clase: String,
    pub licencia: String,
    #[serde(default)]
    pub fichero_url: String,
    #[serde(default)]
    pub licencia_url: String,
    #[serde(default)]
    pub licencia_texto: String,
    #[serde(default)]
    pub puerta: Option<String>,
    /// PaddleOCR no tiene un fichero, una URL: su propia librería descarga
    /// sus pesos la primera vez que se instancia. `true` aquí es la señal
    /// para que la Tarea 6 (worker de descarga) NO intente bajar nada y solo
    /// escriba la licencia — es un caso, no una excepción silenciosa.
    #[serde(default)]
    pub gestion_propia: bool,
}

pub fn cargar_motores(dir: &Path) -> Vec<Motor> {
    leer_dir::<Motor>(dir).into_iter().filter(|m| !m.id.is_empty()).collect()
}
```

Crea los tres ficheros:

`registros/motores/qwen3-vl.json`:
```json
{
  "id": "qwen3-vl",
  "nombre": "Qwen3-VL",
  "clase": "vlm",
  "licencia": "Apache-2.0",
  "fichero_url": "",
  "licencia_url": "https://www.apache.org/licenses/LICENSE-2.0.txt",
  "licencia_texto": "",
  "puerta": null,
  "gestion_propia": false
}
```

`registros/motores/paddleocr.json`:
```json
{
  "id": "paddleocr",
  "nombre": "PaddleOCR",
  "clase": "ocr",
  "licencia": "Apache-2.0",
  "fichero_url": "",
  "licencia_url": "https://www.apache.org/licenses/LICENSE-2.0.txt",
  "licencia_texto": "",
  "puerta": null,
  "gestion_propia": true
}
```

`registros/motores/depth-anything-v2-small.json`:
```json
{
  "id": "depth-anything-v2-small",
  "nombre": "Depth Anything V2 Small",
  "clase": "profundidad",
  "licencia": "Apache-2.0",
  "fichero_url": "",
  "licencia_url": "https://www.apache.org/licenses/LICENSE-2.0.txt",
  "licencia_texto": "",
  "puerta": null,
  "gestion_propia": false
}
```

`fichero_url` queda vacío a propósito: rellenarlo con la URL real de los pesos de Hugging Face es
trabajo manual del propietario, igual que ya lo es `sha256` en `modelos`/`verificadores` — no se
inventa aquí.

- [ ] **Step 3: El manifiesto de `geo`**

`registros/geo/` hoy son ficheros sueltos (`paises.json`, `lado.json`, `koppen.bin`) sin metadatos
de licencia — el texto de atribución vive en prosa dentro de `LEEME.md`. Añade en
`crates/lumi-index/src/geo.rs`, cerca de `pub struct Datos`:

```rust
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct RecursoGeo {
    pub id: String,
    pub nombre: String,
    pub licencia: String,
    #[serde(default)]
    pub fichero_url: String,
    #[serde(default)]
    pub licencia_url: String,
    #[serde(default)]
    pub licencia_texto: String,
    #[serde(default)]
    pub puerta: Option<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize)]
struct RegistroGeo {
    recursos: Vec<RecursoGeo>,
}

pub fn cargar_recursos(dir: &Path) -> Vec<RecursoGeo> {
    std::fs::read(dir.join("registro.json"))
        .ok()
        .and_then(|b| serde_json::from_slice::<RegistroGeo>(&b).ok())
        .map(|r| r.recursos)
        .unwrap_or_default()
}
```

`registros/geo/registro.json`:
```json
{
  "recursos": [
    {
      "id": "paises",
      "nombre": "Natural Earth · países",
      "licencia": "Dominio público",
      "fichero_url": "https://www.naturalearthdata.com/http//www.naturalearthdata.com/download/110m/cultural/ne_110m_admin_0_countries.zip",
      "licencia_url": "https://www.naturalearthdata.com/about/terms-of-use/",
      "licencia_texto": "",
      "puerta": null
    },
    {
      "id": "koppen",
      "nombre": "Köppen-Geiger 0,5°",
      "licencia": "CC BY 4.0",
      "fichero_url": "",
      "licencia_url": "https://creativecommons.org/licenses/by/4.0/legalcode.txt",
      "licencia_texto": "",
      "puerta": null
    }
  ]
}
```

`koppen.fichero_url` queda vacío: Beck et al. no publican un único fichero binario en el formato
exacto que `Koppen::cargar` espera (720×360, un byte ASCII por celda) — es el propietario quien lo
prepara, tal como ya dice `registros/geo/LEEME.md`. La pantalla lo trata como modo guía, igual que
un `puerta` sin `fichero_url`.

- [ ] **Step 4: La prueba de lógica pura**

Al final de `crates/lumi-index/src/registro.rs`:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn motor_sin_id_se_descarta() {
        let dir = tempfile::tempdir().unwrap();
        std::fs::write(dir.path().join("roto.json"), b"{}").unwrap();
        std::fs::write(
            dir.path().join("bueno.json"),
            br#"{"id":"x","nombre":"X","clase":"vlm","licencia":"MIT"}"#,
        )
        .unwrap();
        let motores = cargar_motores(dir.path());
        assert_eq!(motores.len(), 1);
        assert_eq!(motores[0].id, "x");
    }
}
```

Si `tempfile` no está ya en las dev-dependencies de `crates/lumi-index/Cargo.toml`, añádelo:
```toml
[dev-dependencies]
tempfile = "3"
```

- [ ] **Step 5: Compilar y probar**

Run: `cargo build -p lumi-index && cargo test -p lumi-index`
Expected: compila; `motor_sin_id_se_descarta` en verde.

- [ ] **Step 6: Commit**

```bash
git add crates/lumi-index/src/registro.rs crates/lumi-index/src/geo.rs crates/lumi-index/Cargo.toml registros/motores registros/geo/registro.json
git commit -F <(cat <<'EOF'
Los registros aprenden de donde bajan sus pesos, y nace el de motores

Modelo y Verificador ganan fichero_url/licencia_url/licencia_texto/puerta,
y los tres motores compartidos (Qwen3-VL, PaddleOCR, Depth Anything V2)
pasan de estar hardcodeados en Python a tener su propio registro JSON,
con gestion_propia para el caso de PaddleOCR que descarga sus pesos solo.
EOF
)
```

---

### Task 2: `model_licenses`, y el endpoint que las acepta

**Files:**
- Modify: `crates/lumid/src/store.rs`
- Modify: `crates/lumi-proto/src/api.rs`
- Create: `crates/lumid/src/routes/models.rs`
- Modify: `crates/lumid/src/main.rs`

**Interfaces:**
- Consume: `Modelo`/`Verificador`/`Motor`/`RecursoGeo` de la Tarea 1.
- Produce: `POST /v1/admin/models/accept-licenses`, y la función
  `pub fn licencias_aceptadas(app: &App, para: &[String]) -> bool` que la Tarea 6 (arranque de
  descarga) usa para negarse a lanzar una tarea sin aceptación previa.

- [ ] **Step 1: La tabla**

En `crates/lumid/src/store.rs`, añade a la constante `SCHEMA` (junto a las demás
`CREATE TABLE IF NOT EXISTS`):

```sql
CREATE TABLE IF NOT EXISTS model_licenses (
    licencia     TEXT NOT NULL,
    para         TEXT NOT NULL,
    aceptada_por TEXT NOT NULL,
    aceptada_en  INTEGER NOT NULL,
    PRIMARY KEY (licencia, para)
);
```

- [ ] **Step 2: Los tipos**

En `crates/lumi-proto/src/api.rs`:

```rust
#[derive(Debug, Serialize, Deserialize)]
pub struct AcceptLicensesReq {
    /// Una entrada por licencia distinta: `{"MIT": ["anyloc","cliquemining"], ...}`.
    /// Agrupado por texto porque una licencia que cubre dos pesos se acepta
    /// una vez, no dos.
    pub licencias: std::collections::HashMap<String, Vec<String>>,
}
```

- [ ] **Step 3: El endpoint**

Crea `crates/lumid/src/routes/models.rs`:

```rust
//! Rutas de gestión de modelos: aceptar licencias, lanzar descargas,
//! descubrir la tarea activa. La lectura de qué falta por nivel vive en la
//! Tarea 5 (`estado`), no aquí — aceptar y descargar son escritura.

use crate::routes::auth::{bearer, require_admin};
use crate::App;
use axum::extract::State;
use axum::{http::HeaderMap, http::StatusCode, Json};
use lumi_proto::api::AcceptLicensesReq;

fn now() -> i64 {
    std::time::SystemTime::now().duration_since(std::time::UNIX_EPOCH).unwrap().as_secs() as i64
}

pub async fn accept_licenses(
    State(app): State<App>,
    headers: HeaderMap,
    Json(req): Json<AcceptLicensesReq>,
) -> Result<StatusCode, StatusCode> {
    let token = bearer(&headers);
    require_admin(&app, &token)?;
    let quien = crate::routes::auth::session_username(&app, &token).unwrap_or_default();
    let t = now();
    let conn = app.store.conn();
    for (licencia, para) in &req.licencias {
        let junto = para.join(",");
        conn.execute(
            "INSERT OR REPLACE INTO model_licenses (licencia, para, aceptada_por, aceptada_en)
             VALUES (?1, ?2, ?3, ?4)",
            rusqlite::params![licencia, junto, quien, t],
        )
        .map_err(|_| StatusCode::INTERNAL_SERVER_ERROR)?;
    }
    Ok(StatusCode::NO_CONTENT)
}

/// `para` son los ids de lo que una tarea de descarga va a bajar. Se niega a
/// lanzar si falta alguno: es la comprobación que hace cumplirse la §5 de la
/// spec en el servidor, no solo en el gesto de la interfaz.
pub fn licencias_aceptadas(app: &App, para: &[String]) -> bool {
    let conn = app.store.conn();
    let Ok(mut stmt) = conn.prepare("SELECT para FROM model_licenses") else { return false };
    let Ok(filas) = stmt.query_map([], |r| r.get::<_, String>(0)) else { return false };
    let cubiertos: std::collections::HashSet<String> = filas
        .flatten()
        .flat_map(|p| p.split(',').map(str::to_string).collect::<Vec<_>>())
        .collect();
    para.iter().all(|id| cubiertos.contains(id))
}
```

Revisa `crate::routes::auth` — si no existe ya una función que dé el username de la sesión a partir
del bearer token (busca algo como `session_username` o cómo `require_admin` resuelve el usuario),
reutilízala; si el nombre real es distinto, usa el que exista en vez de inventar
`session_username`.

- [ ] **Step 4: Registrar el módulo y la ruta**

En `crates/lumid/src/routes/mod.rs`, añade `pub mod models;` junto a los demás.

En `crates/lumid/src/main.rs`, junto a las rutas de `/v1/admin/...`:
```rust
.route("/v1/admin/models/accept-licenses", post(routes::models::accept_licenses))
```

- [ ] **Step 5: Compilar**

Run: `cargo build --workspace && cargo clippy --workspace --all-targets -- -D warnings`
Expected: verde.

- [ ] **Step 6: Commit**

```bash
git add crates/lumid/src/store.rs crates/lumi-proto/src/api.rs crates/lumid/src/routes/models.rs crates/lumid/src/routes/mod.rs crates/lumid/src/main.rs
git commit -F <(cat <<'EOF'
Aceptar una licencia queda escrito antes de que salga ninguna peticion

Nueva tabla model_licencias y un endpoint que agrupa por texto: la misma
licencia que cubre dos pesos se acepta una vez. licencias_aceptadas() es
la comprobacion que la tarea de descarga (mas adelante) usa para negarse
a arrancar si algo no se acepto, no solo confiar en el gesto de la UI.
EOF
)
```

---

### Task 3: El daemon carga los seis registros, y sabe recargarlos

**Files:**
- Modify: `crates/lumid/src/queue/mod.rs`

**Interfaces:**
- Consume: `cargar_modelos`, `cargar_verificadores`, `cargar_motores` (Tarea 1),
  `RecursoGeo::cargar` (Tarea 1), `cargar_niveles`/`cargar_agentes`/`geo::Datos::cargar` (ya
  existían).
- Produce: `Queue::recargar(&self)`, que la Tarea 6 llama al terminar una descarga; los campos
  `modelos: Mutex<Vec<Modelo>>`, `verificadores: Mutex<Vec<Verificador>>`,
  `motores: Mutex<Vec<Motor>>`, `recursos_geo: Mutex<Vec<RecursoGeo>>` que la Tarea 5 lee para el
  endpoint de estado.

- [ ] **Step 1: Los campos nuevos, y por qué son `Mutex` y no directos**

`niveles`, `agentes` y `geo` en `struct Queue` son valores directos porque hoy nunca cambian tras
arrancar. Este ciclo necesita que SÍ puedan cambiar (recarga en caliente, spec §10), así que los
seis pasan a vivir bajo `Mutex` — sustituye los tres campos existentes y añade los tres nuevos:

```rust
    niveles: Mutex<Vec<lumi_index::niveles::Nivel>>,
    agentes: Mutex<Vec<lumi_index::agentes::Agente>>,
    geo: Mutex<lumi_index::geo::Datos>,
    modelos: Mutex<Vec<lumi_index::registro::Modelo>>,
    verificadores: Mutex<Vec<lumi_index::registro::Verificador>>,
    motores: Mutex<Vec<lumi_index::registro::Motor>>,
    recursos_geo: Mutex<Vec<lumi_index::geo::RecursoGeo>>,
```

Todo lo que hoy lea `cola.niveles`/`cola.agentes`/`cola.geo` directamente (búscalo con
`grep -rn "\.niveles\b\|\.agentes\b\|\.geo\b" crates/lumid/src/` — probablemente en
`crates/lumid/src/routes/queue.rs` o donde se arma la respuesta de un análisis) pasa a
`cola.niveles.lock().unwrap().clone()` (o a leer el campo que haga falta sin clonar todo el vector
si el uso es de un solo elemento — usa el criterio del sitio real, no clones por rutina).

- [ ] **Step 2: `arrancar()` carga los seis**

En `Queue::arrancar`, sustituye la construcción de `niveles`/`agentes`/`geo` y añade los tres
nuevos:

```rust
            niveles: Mutex::new(lumi_index::registro::cargar_niveles(std::path::Path::new("registros/niveles"))),
            agentes: Mutex::new(lumi_index::registro::cargar_agentes(std::path::Path::new("registros/agentes"))),
            geo: Mutex::new(lumi_index::geo::Datos::cargar(std::path::Path::new("registros/geo"))),
            modelos: Mutex::new(lumi_index::registro::cargar_modelos(std::path::Path::new("registros/modelos"))),
            verificadores: Mutex::new(lumi_index::registro::cargar_verificadores(std::path::Path::new("registros/verificadores"))),
            motores: Mutex::new(lumi_index::registro::cargar_motores(std::path::Path::new("registros/motores"))),
            recursos_geo: Mutex::new(lumi_index::geo::RecursoGeo::cargar(std::path::Path::new("registros/geo"))),
```

(Si `RecursoGeo::cargar` no existe como método asociado porque la Tarea 1 lo dejó como función
libre `cargar_recursos`, usa `lumi_index::geo::cargar_recursos(...)` — usa el nombre real que
quedó en el fichero, no el que este plan supone.)

- [ ] **Step 2: `recargar()`**

Añade, en el `impl Queue`:

```rust
    /// Sustituye los seis registros ENTEROS, no los parchea: un análisis en
    /// curso sigue viendo los que tenía cuando empezó, porque su `Arc` a los
    /// datos viejos (si los hubiera capturado) no se toca por esto. Se llama
    /// al terminar una tarea de descarga (Tarea 6) — nunca automáticamente
    /// en un temporizador, porque "recién descargado" es el único momento en
    /// que releer tiene sentido.
    pub fn recargar(&self) {
        *self.niveles.lock().unwrap() =
            lumi_index::registro::cargar_niveles(std::path::Path::new("registros/niveles"));
        *self.agentes.lock().unwrap() =
            lumi_index::registro::cargar_agentes(std::path::Path::new("registros/agentes"));
        *self.geo.lock().unwrap() = lumi_index::geo::Datos::cargar(std::path::Path::new("registros/geo"));
        *self.modelos.lock().unwrap() =
            lumi_index::registro::cargar_modelos(std::path::Path::new("registros/modelos"));
        *self.verificadores.lock().unwrap() =
            lumi_index::registro::cargar_verificadores(std::path::Path::new("registros/verificadores"));
        *self.motores.lock().unwrap() =
            lumi_index::registro::cargar_motores(std::path::Path::new("registros/motores"));
        *self.recursos_geo.lock().unwrap() =
            lumi_index::registro::cargar_recursos(std::path::Path::new("registros/geo"));
        tracing::info!("registros de modelos recargados en caliente");
    }
```

- [ ] **Step 3: Compilar**

Run: `cargo build --workspace`
Expected: los sitios que leían los campos directos ahora fallan a compilar hasta que se les añada
`.lock().unwrap()` — corrígelos uno a uno hasta que compile limpio. No hay atajo: es exactamente
el trabajo de esta tarea.

Run: `cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace`
Expected: verde; el conteo de tests del workspace solo sube en el de la Tarea 1.

- [ ] **Step 4: Commit**

```bash
git add crates/lumid/src/queue/mod.rs
git commit -F <(cat <<'EOF'
Los seis registros pasan a poder releerse sin reiniciar el servidor

niveles/agentes/geo dejan de ser fijos y modelos/verificadores/motores/
recursos_geo se cargan por primera vez (los dos primeros existian en
lumi-index sin que nadie los llamara). Queue::recargar sustituye los seis
enteros, nunca los parchea: un analisis en curso sigue con los que tenia.
EOF
)
```

---

### Task 4: El worker de descarga

**Files:**
- Create: `workers/lumi_bajar.py`

**Interfaces:**
- Consume: nada de Rust todavía (la Tarea 6 lo invoca).
- Produce: un script ejecutable por línea de comandos:
  `python3 lumi_bajar.py <json-de-items>` que escribe a stdout las líneas de log y de progreso
  que la Tarea 6 necesita.

- [ ] **Step 1: El script**

Crea `workers/lumi_bajar.py`:

```python
#!/usr/bin/env python3
"""Descarga pesos, calcula su sha256, escribe la licencia junto a ellos.

Solo biblioteca estandar: es una tarea del runner (crates/lumid/src/tasks.rs),
que ya sabe correr un comando de shell y leer su stdout linea a linea por SSE.
No estructura un protocolo nuevo para un caso — el progreso es una linea mas
del mismo log, con un prefijo reconocible.

Recibe por argv[1] un JSON con la forma:
  [{"id": "anyloc", "fichero_url": "...", "destino": "pesos/anyloc/pesos.pth",
    "licencia_texto": "...", "gestion_propia": false}, ...]

Un item con gestion_propia=true (PaddleOCR) no se descarga: solo se le
escribe LICENCIA.txt. Un item sin fichero_url tampoco se descarga -- no
deberia llegar aqui asi, porque esos quedan en modo guia y el gesto de
"instalar" los excluye del lote antes de lanzar la tarea.
"""
import hashlib
import json
import os
import sys
import urllib.request


def progreso(item_id, hechos, total):
    linea = json.dumps({"item": item_id, "mib": hechos // (1 << 20), "total_mib": total // (1 << 20),
                         "pct": int(hechos * 100 / total) if total else 0})
    print("@progreso " + linea, flush=True)


def descargar(url, destino, item_id):
    os.makedirs(os.path.dirname(destino), exist_ok=True)
    with urllib.request.urlopen(url) as resp:
        total = int(resp.headers.get("Content-Length", 0))
        h = hashlib.sha256()
        hechos = 0
        tmp = destino + ".part"
        with open(tmp, "wb") as f:
            while True:
                trozo = resp.read(1 << 20)
                if not trozo:
                    break
                f.write(trozo)
                h.update(trozo)
                hechos += len(trozo)
                progreso(item_id, hechos, total)
        os.replace(tmp, destino)
        return h.hexdigest()


def main():
    items = json.loads(sys.argv[1])
    total_n = len(items)
    for i, item in enumerate(items, 1):
        item_id = item["id"]
        print(f"[{i}/{total_n}] {item_id}", flush=True)

        if item.get("gestion_propia"):
            # PaddleOCR: su propia libreria baja sus pesos la primera vez que
            # se instancia. Aqui solo se deja constancia de la licencia.
            directorio = os.path.dirname(item["destino"])
            os.makedirs(directorio, exist_ok=True)
            with open(os.path.join(directorio, "LICENCIA.txt"), "w") as f:
                f.write(item["licencia_texto"])
            print(f"      gestion propia: licencia escrita, la libreria trae sus pesos sola", flush=True)
            continue

        destino = item["destino"]
        real = descargar(item["fichero_url"], destino, item_id)

        esperado = item.get("sha256", "")
        if esperado:
            if real != esperado:
                os.remove(destino)
                print(f"FATAL sha256 no coincide en {item_id}: esperado {esperado}, real {real}", flush=True)
                sys.exit(1)
            print(f"      sha256 ok  hash conocido", flush=True)
        else:
            print(f"      el registro no traia sha256; se anota el de lo descargado", flush=True)
            print(f"      verificado solo contra si mismo", flush=True)

        directorio = os.path.dirname(destino)
        with open(os.path.join(directorio, "LICENCIA.txt"), "w") as f:
            f.write(item["licencia_texto"])
        print(f"      licencia escrita en {directorio}/LICENCIA.txt", flush=True)

        # El hash real se imprime en una linea que Rust reconoce y usa para
        # reescribir el registro (Tarea 6) — igual que el progreso, sin
        # inventar un segundo canal.
        print("@sha256 " + json.dumps({"item": item_id, "sha256": real}), flush=True)

    print("hecho", flush=True)


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Probarlo a mano contra un fichero pequeño**

Run:
```bash
python3 workers/lumi_bajar.py '[{"id":"prueba","fichero_url":"https://raw.githubusercontent.com/torvalds/linux/master/COPYING","destino":"/tmp/prueba/pesos.pth","licencia_texto":"texto de prueba","gestion_propia":false}]'
```
Expected: líneas `@progreso ...`, luego `sha256 no coincide` O `el registro no traia sha256` (no
se pasó `sha256` a propósito, así que debe caer en la rama "solo contra sí mismo"), termina con
`hecho`, y `/tmp/prueba/LICENCIA.txt` existe con "texto de prueba".

- [ ] **Step 3: Commit**

```bash
git add workers/lumi_bajar.py
git commit -F <(cat <<'EOF'
Un script que descarga, verifica y deja la licencia donde lumi_pesos la busca

Solo biblioteca estandar, igual que los demas workers. El progreso y el
sha256 real salen como lineas de log con un prefijo propio (@progreso,
@sha256) en vez de un protocolo nuevo: es el mismo log que ya llega por
SSE. gestion_propia cubre el caso de PaddleOCR, que descarga sus pesos
solo -- aqui solo se le escribe la licencia.
EOF
)
```

---

### Task 5: `TaskKind::ModelDownload`, y el id de la tarea activa

**Files:**
- Modify: `crates/lumi-proto/src/api.rs`
- Modify: `crates/lumid/src/tasks.rs`
- Modify: `client/src/lib/api.ts`

**Interfaces:**
- Consume: `licencias_aceptadas` (Tarea 2), `workers/lumi_bajar.py` (Tarea 4).
- Produce: `tasks::spawn_model_download(app: &App, items: Vec<ItemDescarga>) -> Result<String>`,
  y el meta-key `model_task_id` que la Tarea 7 (endpoint de descubrimiento) y la Tarea 8 (recarga
  al terminar) leen.

- [ ] **Step 1: El `TaskKind` nuevo**

En `crates/lumi-proto/src/api.rs`:

```rust
#[derive(Debug, Clone, Copy, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum TaskKind {
    InferenceRuntime,
    Database,
    ModelDownload,
}
```

En `client/src/lib/api.ts`, ensancha la unión existente:
```ts
export interface TaskStatus {
  id: string;
  kind: "inference_runtime" | "database" | "model_download";
  running: boolean;
  exit_code: number | null;
  log_len: number;
}
```

- [ ] **Step 2: `ItemDescarga`, y `spawn_model_download`**

En `crates/lumi-proto/src/api.rs`:
```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ItemDescarga {
    pub id: String,
    pub fichero_url: String,
    pub destino: String,
    pub licencia_texto: String,
    pub sha256: String,
    pub gestion_propia: bool,
}
```

En `crates/lumid/src/tasks.rs`, el `command()` gana un brazo — `ModelDownload` no encaja en la
forma "un comando fijo" que tienen los otros dos, porque necesita pasar la lista de items como
argumento. Cambia la firma de `command` para aceptar un payload opcional:

```rust
fn command(kind: TaskKind, dir: &Path, models_dir: Option<&str>, payload: Option<&str>) -> (String, Vec<String>) {
    let base = models_dir.map(PathBuf::from).unwrap_or_else(|| dir.join("runtime"));
    let venv = base.join("venv");
    match kind {
        TaskKind::InferenceRuntime => ( /* sin cambios */ ),
        TaskKind::Database => ( /* sin cambios */ ),
        TaskKind::ModelDownload => (
            venv.join("bin").join("python3").to_string_lossy().into_owned(),
            vec!["workers/lumi_bajar.py".into(), payload.unwrap_or("[]").into()],
        ),
    }
}
```

Actualiza la única llamada a `command(...)` dentro de `spawn` para pasar el nuevo parámetro
(`None` para los dos tipos que no lo usan):

```rust
pub fn spawn(app: &App, kind: TaskKind) -> Result<String> {
    spawn_con_payload(app, kind, None)
}

pub fn spawn_model_download(app: &App, items: Vec<lumi_proto::api::ItemDescarga>) -> Result<String> {
    let payload = serde_json::to_string(&items)?;
    spawn_con_payload(app, TaskKind::ModelDownload, Some(payload))
}

fn spawn_con_payload(app: &App, kind: TaskKind, payload: Option<String>) -> Result<String> {
    let id = crate::routes::claim::new_token()[..12].to_string();
    std::fs::create_dir_all(app.dir.join("tasks"))?;
    let path = log_path(&app.dir, &id);
    std::fs::File::create(&path)?;

    app.store.conn().execute(
        "INSERT INTO tasks (id, kind, running, exit_code, started_at) VALUES (?1, ?2, 1, NULL, ?3)",
        rusqlite::params![id, serde_json::to_string(&kind)?, now()],
    )?;

    if kind == TaskKind::ModelDownload {
        app.store.set_meta("model_task_id", &id)?;
    }

    let models_dir = app.store.get_meta("models_dir");
    let (bin, args) = command(kind, &app.dir, models_dir.as_deref(), payload.as_deref());
    // el resto del cuerpo (tokio::spawn del proceso, lectura de stdout/stderr) no cambia
    ...
}
```

Copia el cuerpo real de `tokio::spawn(...)` que ya existe en `spawn` sin modificarlo — el único
cambio es de dónde vienen `bin`/`args` y la línea nueva de `set_meta` antes de lanzarlo.

En `finish(...)`, añade la limpieza del meta-key cuando la tarea que termina es una descarga de
modelos (necesitas pasarle el `kind`, o consultarlo antes de llamar a `finish`):

```rust
fn finish(store: &crate::store::Store, id: &str, code: Option<i32>, kind: TaskKind) {
    let _ = store.conn().execute(
        "UPDATE tasks SET running = 0, exit_code = ?2 WHERE id = ?1",
        rusqlite::params![id, code],
    );
    if kind == TaskKind::ModelDownload {
        let _ = store.conn().execute("DELETE FROM meta WHERE k = 'model_task_id'", []);
    }
}
```

Actualiza las tres llamadas a `finish(&store, &id2, ...)` dentro del `tokio::spawn` para pasar
`kind` también (captúralo en el closure junto con `store`/`id2`).

- [ ] **Step 3: Compilar**

Run: `cargo build --workspace && cargo clippy --workspace --all-targets -- -D warnings`
Expected: verde. `cargo build -p client` no aplica (TypeScript) — pasa a `npm run build` en la
Tarea 11, cuando el cliente ya use estos tipos.

- [ ] **Step 4: Commit**

```bash
git add crates/lumi-proto/src/api.rs crates/lumid/src/tasks.rs client/src/lib/api.ts
git commit -F <(cat <<'EOF'
La descarga de modelos es un tercer tipo de tarea del mismo runner

ModelDownload lleva su lista de items como argumento en vez de un comando
fijo, asi que command() gana un payload opcional. El id de la tarea vive
en meta bajo model_task_id mientras corre, para que cualquier pestana
que se acabe de abrir la descubra sin depender del navegador que la
lanzo.
EOF
)
```

---

### Task 6: Lanzar la descarga, y recargar al terminar

**Files:**
- Modify: `crates/lumid/src/routes/models.rs`
- Modify: `crates/lumid/src/main.rs`

**Interfaces:**
- Consume: `licencias_aceptadas` (Tarea 2), `spawn_model_download` (Tarea 5), `Queue::recargar`
  (Tarea 3), los seis registros de la Tarea 3 (para resolver `ItemDescarga` a partir de un id).
- Produce: `POST /v1/admin/models/download` `{ items: string[] }` → `TaskStatus`.

- [ ] **Step 1: Resolver ids a `ItemDescarga`**

En `crates/lumid/src/routes/models.rs`, añade:

```rust
use lumi_proto::api::ItemDescarga;

fn resolver_items(app: &App, ids: &[String]) -> Vec<ItemDescarga> {
    let modelos = app.queue.modelos.lock().unwrap().clone();
    let verificadores = app.queue.verificadores.lock().unwrap().clone();
    let motores = app.queue.motores.lock().unwrap().clone();
    let recursos_geo = app.queue.recursos_geo.lock().unwrap().clone();
    let models_dir = app.store.get_meta("models_dir").unwrap_or_else(|| "runtime/pesos".to_string());

    let mut fuera = Vec::new();
    for id in ids {
        if let Some(m) = modelos.iter().find(|m| &m.id == id) {
            fuera.push(ItemDescarga {
                id: m.id.clone(), fichero_url: m.fichero_url.clone(),
                destino: format!("{models_dir}/{}/pesos.pth", m.id),
                licencia_texto: m.licencia_texto.clone(), sha256: m.sha256.clone(),
                gestion_propia: false,
            });
        } else if let Some(v) = verificadores.iter().find(|v| &v.id == id) {
            fuera.push(ItemDescarga {
                id: v.id.clone(), fichero_url: v.fichero_url.clone(),
                destino: format!("{models_dir}/{}/pesos.pth", v.id),
                licencia_texto: v.licencia_texto.clone(), sha256: v.sha256.clone(),
                gestion_propia: false,
            });
        } else if let Some(mo) = motores.iter().find(|mo| &mo.id == id) {
            fuera.push(ItemDescarga {
                id: mo.id.clone(), fichero_url: mo.fichero_url.clone(),
                destino: format!("{models_dir}/{}/model.safetensors", mo.id),
                licencia_texto: mo.licencia_texto.clone(), sha256: String::new(),
                gestion_propia: mo.gestion_propia,
            });
        } else if let Some(g) = recursos_geo.iter().find(|g| &g.id == id) {
            fuera.push(ItemDescarga {
                id: g.id.clone(), fichero_url: g.fichero_url.clone(),
                destino: format!("registros/geo/{}", if g.id == "paises" { "paises.json" } else { "koppen.bin" }),
                licencia_texto: g.licencia_texto.clone(), sha256: String::new(),
                gestion_propia: false,
            });
        }
    }
    fuera
}
```

- [ ] **Step 2: El endpoint**

```rust
#[derive(serde::Deserialize)]
pub struct DownloadReq {
    pub items: Vec<String>,
}

pub async fn download(
    State(app): State<App>,
    headers: HeaderMap,
    Json(req): Json<DownloadReq>,
) -> Result<Json<lumi_proto::api::TaskStatus>, (StatusCode, String)> {
    require_admin(&app, &bearer(&headers))
        .map_err(|c| (c, "hace falta ser administrador".to_string()))?;

    if !licencias_aceptadas(&app, &req.items) {
        return Err((StatusCode::BAD_REQUEST, "faltan licencias por aceptar para lo que se pidió".to_string()));
    }

    let items = resolver_items(&app, &req.items);
    if items.iter().any(|i| i.fichero_url.is_empty() && !i.gestion_propia) {
        return Err((StatusCode::BAD_REQUEST, "alguno de estos pesos no tiene URL de descarga: modo guía, no se puede pedir aquí".to_string()));
    }

    let id = crate::tasks::spawn_model_download(&app, items)
        .map_err(|e| (StatusCode::INTERNAL_SERVER_ERROR, e.to_string()))?;
    let cola = app.queue.clone();
    tokio::spawn(async move {
        // Sondea hasta que la tarea termine y entonces recarga — el propio
        // spawn de tasks.rs no sabe nada de niveles/registros, y no debería:
        // ese acoplamiento es cosa de quien pidió la descarga, no del runner
        // genérico.
        loop {
            tokio::time::sleep(std::time::Duration::from_secs(2)).await;
            let Some(status) = crate::tasks::status(&crate::App { queue: cola.clone(), ..unreachable!() }, "") else { break };
            if !status.running { break; }
        }
    });
    crate::tasks::status(&app, &id)
        .map(Json)
        .ok_or((StatusCode::INTERNAL_SERVER_ERROR, "no se pudo leer el estado recién creado".to_string()))
}
```

El bloque `tokio::spawn` de arriba tiene un `unreachable!()` que NO compila — es intencional que
lo reescribas: `crate::tasks::status` pide un `&App` completo y este handler solo tiene `Arc<Queue>`
disponible tras moverlo al closure. La forma correcta es clonar `app` entero (ya es barato: es
`Arc`s por dentro, revisa `#[derive(Clone)]` en `struct App` o añádelo si falta) y sondear con
`app.clone()` dentro del `tokio::spawn`, llamando a `app.queue.recargar()` cuando
`!status.running`:

```rust
    let app2 = app.clone();
    let id2 = id.clone();
    tokio::spawn(async move {
        loop {
            tokio::time::sleep(std::time::Duration::from_secs(2)).await;
            match crate::tasks::status(&app2, &id2) {
                Some(s) if !s.running => { app2.queue.recargar(); break; }
                Some(_) => continue,
                None => break,
            }
        }
    });
```

Si `App` no deriva `Clone` hoy, añade `#[derive(Clone)]` en `crates/lumid/src/main.rs` — todos sus
campos ya son `Arc`/`PathBuf`/tipos `Copy`, así que clonar `App` es barato y ya es el patrón que
axum espera de un `State<App>` (axum ya exige que `App` sea `Clone` para poder compartirlo entre
handlers — probablemente ya lo tiene; solo confírmalo).

- [ ] **Step 3: Registrar la ruta**

En `crates/lumid/src/main.rs`:
```rust
.route("/v1/admin/models/download", post(routes::models::download))
```

- [ ] **Step 4: Compilar**

Run: `cargo build --workspace && cargo clippy --workspace --all-targets -- -D warnings`
Expected: verde.

- [ ] **Step 5: Commit**

```bash
git add crates/lumid/src/routes/models.rs crates/lumid/src/main.rs
git commit -F <(cat <<'EOF'
Instalar un nivel se niega a arrancar sin sus licencias aceptadas

download() resuelve los ids pedidos contra los seis registros, se niega
si alguno esta en modo guia (sin fichero_url) o sin licencia aceptada, y
lanza la tarea. Un vigilante en segundo plano recarga los registros en
cuanto la tarea termina -- recargar() sustituye entero, nunca parchea.
EOF
)
```

---

### Task 7: Descubrir la tarea activa, y el estado de niveles

**Files:**
- Modify: `crates/lumid/src/routes/models.rs`
- Modify: `crates/lumi-proto/src/api.rs`
- Modify: `crates/lumid/src/main.rs`
- Test: `crates/lumi-index/src/niveles.rs` (módulo `#[cfg(test)]`)

**Interfaces:**
- Consume: `model_task_id` (Tarea 5), los seis registros (Tarea 3).
- Produce: `GET /v1/admin/model-task` → `Option<TareaModelo>`; `GET /v1/admin/models` →
  `EstadoModelos`; la función pura `lumi_index::niveles::resolver(nivel, instalados) -> Resolucion`
  que ambos endpoints usan y que la prueba cubre.

- [ ] **Step 1: La función pura — qué falta por nivel, sin contar dos veces lo compartido**

En `crates/lumi-index/src/niveles.rs`:

```rust
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct Resolucion {
    pub recuperacion_instalados: usize,
    pub recuperacion_total: usize,
    pub geometricos_instalados: usize,
    pub geometricos_total: usize,
    /// Ids que faltan y que ESTE nivel necesita descargar — no incluye lo
    /// que otro nivel ya trajo, que se marca compartido en la interfaz
    /// leyendo `instalados` directamente, no aquí.
    pub faltan: Vec<String>,
}

/// `instalados` es el conjunto de ids con sha256 verificado en disco — lo
/// calcula quien llama (el endpoint, con acceso al filesystem), no esta
/// función: aquí solo hay aritmética de conjuntos, que es lo único que la
/// prueba necesita fijar.
pub fn resolver(nivel: &Nivel, instalados: &std::collections::HashSet<String>) -> Resolucion {
    let todos: Vec<&String> = nivel.recuperacion.iter().chain(nivel.geometricos.iter()).collect();
    let faltan: Vec<String> = todos.iter().filter(|id| !instalados.contains(id.as_str())).map(|s| s.to_string()).collect();
    Resolucion {
        recuperacion_instalados: nivel.recuperacion.iter().filter(|id| instalados.contains(id.as_str())).count(),
        recuperacion_total: nivel.recuperacion.len(),
        geometricos_instalados: nivel.geometricos.iter().filter(|id| instalados.contains(id.as_str())).count(),
        geometricos_total: nivel.geometricos.len(),
        faltan,
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use std::collections::HashSet;

    #[test]
    fn compartido_no_se_cuenta_dos_veces_como_falta() {
        let nivel = Nivel {
            id: "vision".into(), nombre: "Vision".into(),
            recuperacion: vec!["cosplace".into(), "salad".into()],
            geometricos: vec!["roma".into()],
            agentes: vec![], cae_a: Some("pro".into()),
        };
        let instalados: HashSet<String> = ["cosplace".into()].into_iter().collect();
        let r = resolver(&nivel, &instalados);
        assert_eq!(r.recuperacion_instalados, 1);
        assert_eq!(r.faltan, vec!["salad".to_string()]);
        assert!(!r.faltan.contains(&"cosplace".to_string()));
    }
}
```

- [ ] **Step 2: `TareaModelo` y el endpoint de descubrimiento**

En `crates/lumi-proto/src/api.rs`:
```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct TareaModelo {
    pub id: String,
    pub item_actual: Option<String>,
    pub pct: Option<u32>,
}
```

En `crates/lumid/src/routes/models.rs`:
```rust
pub async fn model_task(
    State(app): State<App>,
    headers: HeaderMap,
) -> Result<Json<Option<lumi_proto::api::TareaModelo>>, StatusCode> {
    require_admin(&app, &bearer(&headers))?;
    let Some(id) = app.store.get_meta("model_task_id") else { return Ok(Json(None)) };
    let Some(status) = crate::tasks::status(&app, &id) else { return Ok(Json(None)) };
    if !status.running {
        return Ok(Json(None));
    }
    // Última línea @progreso del log, para el ítem y el porcentaje actuales
    // — el mismo log que ya se sirve por SSE, sin protocolo nuevo.
    let log = std::fs::read_to_string(crate::tasks::log_path(&app.dir, &id)).unwrap_or_default();
    let ultima = log.lines().rev().find(|l| l.starts_with("@progreso "));
    let (item_actual, pct) = ultima
        .and_then(|l| serde_json::from_str::<serde_json::Value>(&l[10..]).ok())
        .map(|v| (
            v.get("item").and_then(|x| x.as_str()).map(str::to_string),
            v.get("pct").and_then(|x| x.as_u64()).map(|p| p as u32),
        ))
        .unwrap_or((None, None));
    Ok(Json(Some(lumi_proto::api::TareaModelo { id, item_actual, pct })))
}
```

- [ ] **Step 3: `GET /v1/admin/models` — estado agregado**

```rust
#[derive(serde::Serialize)]
pub struct NivelEstado {
    pub id: String,
    pub nombre: String,
    pub resolucion: lumi_index::niveles::Resolucion,
}

pub async fn estado(
    State(app): State<App>,
    headers: HeaderMap,
) -> Result<Json<Vec<NivelEstado>>, StatusCode> {
    require_admin(&app, &bearer(&headers))?;
    let niveles = app.queue.niveles.lock().unwrap().clone();
    let modelos_dir = app.store.get_meta("models_dir").unwrap_or_else(|| "runtime/pesos".to_string());

    // Instalado = LICENCIA.txt presente junto al peso — el mismo criterio
    // que lumi_pesos._licencia exige para cargar, así que "instalado" aquí
    // nunca puede decir sí cuando Python diría que no.
    let instalados: std::collections::HashSet<String> = std::fs::read_dir(&modelos_dir)
        .map(|rd| {
            rd.flatten()
                .filter(|e| e.path().join("LICENCIA.txt").exists())
                .filter_map(|e| e.file_name().into_string().ok())
                .collect()
        })
        .unwrap_or_default();

    let fuera = niveles
        .iter()
        .map(|n| NivelEstado {
            id: n.id.clone(), nombre: n.nombre.clone(),
            resolucion: lumi_index::niveles::resolver(n, &instalados),
        })
        .collect();
    Ok(Json(fuera))
}
```

- [ ] **Step 4: Registrar las rutas**

```rust
.route("/v1/admin/model-task", get(routes::models::model_task))
.route("/v1/admin/models", get(routes::models::estado))
```

- [ ] **Step 5: Compilar y probar**

Run: `cargo build --workspace && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace`
Expected: verde; `compartido_no_se_cuenta_dos_veces_como_falta` en verde.

- [ ] **Step 6: Commit**

```bash
git add crates/lumi-index/src/niveles.rs crates/lumi-proto/src/api.rs crates/lumid/src/routes/models.rs crates/lumid/src/main.rs
git commit -F <(cat <<'EOF'
Una pestana recien abierta descubre la tarea activa sin memoria propia

resolver() es la aritmetica de que falta por nivel sin contar dos veces
lo compartido, probada con datos fijos. model_task lee model_task_id de
meta y la ultima linea @progreso del log; estado() agrega los niveles
contra lo que de verdad hay en disco (LICENCIA.txt presente, el mismo
criterio que ya exige lumi_pesos para cargar).
EOF
)
```

---

### Task 8: El token del proveedor, cuando pone puerta

**Files:**
- Modify: `crates/lumi-proto/src/api.rs`
- Modify: `crates/lumid/src/routes/models.rs`
- Modify: `crates/lumid/src/main.rs`
- Modify: `client/src/lib/api.ts`

**Interfaces:**
- Consume: nada nuevo.
- Produce: `GET /v1/admin/models/provider-token` → `{ has_token: bool }`;
  `PATCH /v1/admin/models/provider-token` `{ token: string | null }`.

- [ ] **Step 1: Los tipos**

En `crates/lumi-proto/src/api.rs`:
```rust
#[derive(Debug, Serialize, Deserialize)]
pub struct ProviderTokenState {
    pub has_token: bool,
}

#[derive(Debug, Serialize, Deserialize)]
pub struct ProviderTokenReq {
    /// `None` no toca el token guardado — igual que `MapConfigReq.key`, para
    /// poder guardar sin tener que volver a teclear uno que ya estaba.
    /// `Some("")` sí lo borra: es la forma de quitarlo del todo.
    pub token: Option<String>,
}
```

- [ ] **Step 2: Las rutas — mismo patrón exacto que `map_key`**

En `crates/lumid/src/routes/models.rs`:
```rust
// ponytail: en claro en `meta`, nunca devuelto al cliente — el mismo
// riesgo y el mismo tratamiento que `map_key` en routes/map.rs. Cifrar
// esto y no aquello sería proteger un secreto y no el otro sin ninguna
// diferencia real de amenaza entre los dos.
pub async fn get_provider_token(
    State(app): State<App>,
    headers: HeaderMap,
) -> Result<Json<lumi_proto::api::ProviderTokenState>, StatusCode> {
    require_admin(&app, &bearer(&headers))?;
    Ok(Json(lumi_proto::api::ProviderTokenState {
        has_token: app.store.get_meta("model_provider_token").is_some(),
    }))
}

pub async fn set_provider_token(
    State(app): State<App>,
    headers: HeaderMap,
    Json(req): Json<lumi_proto::api::ProviderTokenReq>,
) -> Result<Json<lumi_proto::api::ProviderTokenState>, StatusCode> {
    require_admin(&app, &bearer(&headers))?;
    if let Some(t) = req.token {
        if t.is_empty() {
            let _ = app.store.conn().execute("DELETE FROM meta WHERE k = 'model_provider_token'", []);
        } else {
            app.store.set_meta("model_provider_token", t.trim()).map_err(|_| StatusCode::INTERNAL_SERVER_ERROR)?;
        }
    }
    Ok(Json(lumi_proto::api::ProviderTokenState {
        has_token: app.store.get_meta("model_provider_token").is_some(),
    }))
}
```

El worker (Tarea 4) no usa este token todavía porque hoy solo RoMa v2 tiene `puerta`, y su
descarga real (pedirle a Hugging Face con el token en la cabecera `Authorization`) es trabajo
futuro del propio propietario cuando rellene `fichero_url` — este ciclo deja el token guardado y
disponible, no automatiza la petición autenticada. Anótalo así en el commit para que no se lea
como un olvido.

- [ ] **Step 3: Registrar rutas**

```rust
.route("/v1/admin/models/provider-token", get(routes::models::get_provider_token).patch(routes::models::set_provider_token))
```

- [ ] **Step 4: El tipo en el cliente**

En `client/src/lib/api.ts`:
```ts
export interface ProviderTokenState {
  has_token: boolean;
}
```

- [ ] **Step 5: Compilar**

Run: `cargo build --workspace && cargo clippy --workspace --all-targets -- -D warnings`
Expected: verde.

- [ ] **Step 6: Commit**

```bash
git add crates/lumi-proto/src/api.rs crates/lumid/src/routes/models.rs crates/lumid/src/main.rs client/src/lib/api.ts
git commit -F <(cat <<'EOF'
El token del proveedor de pesos se guarda como ya se guarda el de mapas

Mismo contrato que map_key: en claro en meta, nunca devuelto al cliente,
solo un has_token booleano. No automatiza la peticion autenticada a
Hugging Face -- eso depende de que el propietario rellene fichero_url
para RoMa v2, que sigue siendo trabajo manual suyo.
EOF
)
```

---

### Task 9: `ModelosView` — la pantalla de niveles

**Files:**
- Create: `client/src/admin/ModelosView.tsx`
- Modify: `client/src/admin/AdminPanel.tsx`
- Modify: `client/src/admin/Hueco.tsx`
- Modify: `client/src/lib/api.ts`

**Interfaces:**
- Consume: `GET /v1/admin/models` (Tarea 7), `Seccion` (componente, de `AdminPanel.tsx`).
- Produce: `ModelosView({ token, nivelInicial? }: { token: string; nivelInicial?: string })` — la
  Tarea 11 (wizard) y la Tarea 12 (toasts) lo montan con un nivel ya elegido.

- [ ] **Step 1: Los tipos de respuesta**

En `client/src/lib/api.ts`:
```ts
export interface Resolucion {
  recuperacion_instalados: number;
  recuperacion_total: number;
  geometricos_instalados: number;
  geometricos_total: number;
  faltan: string[];
}
export interface NivelEstado {
  id: string;
  nombre: string;
  resolucion: Resolucion;
}
```

- [ ] **Step 2: La vista**

Crea `client/src/admin/ModelosView.tsx` copiando la estructura y clases EXACTAS de
`docs/superpowers/specs/lumi-s3a-mockups.html`, sección 2 (`.lvl`, `.meter`, `.cols`, `.col li`
con el `.dot` de apagado, `.dl` para la barra de descarga) — el mockup ya está aprobado, esto es
traducirlo a componente, no rediseñarlo:

```tsx
import { useEffect, useState } from "react";
import { api, type NivelEstado } from "../lib/api";
import { Seccion } from "./AdminPanel";

export function ModelosView({ token, nivelInicial }: { token: string; nivelInicial?: string }) {
  const [niveles, setNiveles] = useState<NivelEstado[] | null>(null);
  const [abierto, setAbierto] = useState<string | null>(nivelInicial ?? null);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    api.get<NivelEstado[]>("/v1/admin/models", token)
      .then(setNiveles)
      .catch((e) => setError(String(e)));
  }, [token]);

  return (
    <Seccion titulo="Modelos e inferencia" grupo="Servidor">
      {error && <p className="text-[11px] text-danger-fg">{error}</p>}
      {niveles === null && !error && <p className="text-[11px] text-muted">cargando</p>}

      <div className="flex flex-col gap-3">
        {(niveles ?? []).map((n) => {
          const total = n.resolucion.recuperacion_total + n.resolucion.geometricos_total;
          const instalado = n.resolucion.recuperacion_instalados + n.resolucion.geometricos_instalados;
          const completo = n.resolucion.faltan.length === 0;
          const abiertoAqui = abierto === n.id;
          return (
            <div key={n.id}
              className={`rounded-[11px] border bg-panel transition-colors duration-[400ms] ease-expo
                ${abiertoAqui ? "border-white/20" : "border-border hover:border-white/[.16]"}`}>
              <button onClick={() => setAbierto(abiertoAqui ? null : n.id)}
                className="grid w-full grid-cols-[1fr_auto] items-center gap-3.5 px-[15px] py-[13px] text-left">
                <div>
                  <h4 className="flex items-center gap-2.5 text-[13.5px] font-medium">
                    {n.nombre}
                    <span className={`rounded-[5px] border px-1.5 py-px text-[8.5px] tracking-[.05em]
                      ${completo ? "border-white/[.28] text-fg" : "border-warning/40 text-warning-fg"}`}>
                      {completo ? "listo" : `falta ${n.resolucion.faltan.length}`}
                    </span>
                  </h4>
                  <div className="mt-[3px] text-[10.5px] text-muted">
                    {n.resolucion.recuperacion_total} recuperadores · {n.resolucion.geometricos_total} verificadores
                  </div>
                  <div className="mt-[9px] h-[3px] overflow-hidden rounded-sm bg-elevated">
                    <div className="h-full bg-fg transition-[width] duration-1000 ease-expo"
                      style={{ width: `${total ? Math.round((instalado / total) * 100) : 100}%` }} />
                  </div>
                </div>
                {!completo && (
                  <button onClick={(e) => { e.stopPropagation(); /* Tarea 10 conecta esto a la rejilla de licencias */ }}
                    className="rounded-lg bg-accent px-3 py-1.5 text-[11px] font-medium text-black">
                    Completar {n.nombre}
                  </button>
                )}
              </button>

              <div className={`grid transition-[grid-template-rows] duration-[550ms] ease-expo
                ${abiertoAqui ? "grid-rows-[1fr]" : "grid-rows-[0fr]"}`}>
                <div className="overflow-hidden">
                  <div className="border-t border-border px-[15px] py-3">
                    <p className="text-[10.5px] text-subtle">
                      {n.resolucion.faltan.length === 0
                        ? "Todo instalado en este nivel."
                        : `Falta: ${n.resolucion.faltan.join(", ")}`}
                    </p>
                  </div>
                </div>
              </div>
            </div>
          );
        })}
      </div>
    </Seccion>
  );
}
```

Este componente cubre el listado de niveles con su medidor (mockup §2, primer nivel de detalle).
La rejilla de recuperadores/verificadores COMPARTIDO/apagado por columna, la licencia y la puerta
son la Tarea 10 — no las repitas aquí, `ModelosView` solo orquesta qué nivel está abierto.

- [ ] **Step 3: Conectarla en `AdminPanel`**

En `client/src/admin/AdminPanel.tsx`, quita `"modelos"` de `PRONTO`:
```tsx
const PRONTO: Seccion[] = ["mantenimiento", "notificaciones", "hardware"];
```

Añade la rama real en el ternario de secciones, junto a `claves`:
```tsx
          : seccion === "modelos" ? <ModelosView token={token} />
```

Y el import:
```tsx
import { ModelosView } from "./ModelosView";
```

En `client/src/admin/Hueco.tsx`, borra la entrada `modelos` del objeto `QUE` — ya no es un hueco.

- [ ] **Step 4: Comprobar**

Run: `cd client && npm run lint && npm run build`
Expected: verde.

- [ ] **Step 5: Commit**

```bash
git add client/src/admin/ModelosView.tsx client/src/admin/AdminPanel.tsx client/src/admin/Hueco.tsx client/src/lib/api.ts
git commit -F <(cat <<'EOF'
Modelos deja de ser un hueco con "pronto"

ModelosView pinta los tres niveles con su medidor real contra
GET /v1/admin/models, tal como aprobo el mockup. La rejilla de pesos por
columna y la rejilla de licencias son la siguiente tarea -- esta solo
saca la seccion de PRONTO y la conecta al endpoint de estado.
EOF
)
```

---

### Task 10: La rejilla de pesos, las licencias y la puerta

**Files:**
- Modify: `client/src/admin/ModelosView.tsx`
- Create: `client/src/admin/LicenciasGate.tsx`
- Modify: `client/src/admin/KeysView.tsx`
- Modify: `client/src/lib/api.ts`

**Interfaces:**
- Consume: `ModelosView` (Tarea 9), `POST /v1/admin/models/accept-licenses` (Tarea 2),
  `POST /v1/admin/models/download` (Tarea 6), `PATCH /v1/admin/models/provider-token` (Tarea 8).
- Produce: `LicenciasGate({ token, items, onListo }: { token: string; items: PesoPendiente[];
  onListo: () => void })`.

- [ ] **Step 1: El tipo que agrupa lo pendiente**

En `client/src/lib/api.ts`:
```ts
export interface PesoPendiente {
  id: string;
  nombre: string;
  licencia: string;
  licencia_texto: string;
  puerta: string | null;
}
```

Este tipo lo arma el cliente combinando lo que ya conoce (`NivelEstado.resolucion.faltan` da los
ids) contra un catálogo de metadatos — añade un cuarto endpoint de solo lectura para no tener que
enviar el registro entero al cliente. En `crates/lumid/src/routes/models.rs` (Rust, no
TypeScript — este paso vuelve atrás a Rust porque el cliente necesita nombre/licencia/puerta de
cada id pendiente y hoy no hay ruta que lo dé):

```rust
#[derive(serde::Serialize)]
pub struct MetaPeso {
    pub id: String,
    pub nombre: String,
    pub licencia: String,
    pub licencia_texto: String,
    pub puerta: Option<String>,
}

pub async fn metadatos(
    State(app): State<App>,
    headers: HeaderMap,
) -> Result<Json<Vec<MetaPeso>>, StatusCode> {
    require_admin(&app, &bearer(&headers))?;
    let mut fuera = Vec::new();
    for m in app.queue.modelos.lock().unwrap().iter() {
        fuera.push(MetaPeso { id: m.id.clone(), nombre: m.nombre.clone(), licencia: m.licencia.clone(), licencia_texto: m.licencia_texto.clone(), puerta: m.puerta.clone() });
    }
    for v in app.queue.verificadores.lock().unwrap().iter() {
        fuera.push(MetaPeso { id: v.id.clone(), nombre: v.nombre.clone(), licencia: v.licencia.clone(), licencia_texto: v.licencia_texto.clone(), puerta: v.puerta.clone() });
    }
    for mo in app.queue.motores.lock().unwrap().iter() {
        fuera.push(MetaPeso { id: mo.id.clone(), nombre: mo.nombre.clone(), licencia: mo.licencia.clone(), licencia_texto: mo.licencia_texto.clone(), puerta: mo.puerta.clone() });
    }
    for g in app.queue.recursos_geo.lock().unwrap().iter() {
        fuera.push(MetaPeso { id: g.id.clone(), nombre: g.nombre.clone(), licencia: g.licencia.clone(), licencia_texto: g.licencia_texto.clone(), puerta: g.puerta.clone() });
    }
    Ok(Json(fuera))
}
```

Registra en `main.rs`: `.route("/v1/admin/models/metadata", get(routes::models::metadatos))`.

Run: `cargo build --workspace && cargo clippy --workspace --all-targets -- -D warnings` — verde
antes de seguir con TypeScript.

- [ ] **Step 2: `LicenciasGate` — rejilla, lector, aceptar, y la puerta**

Crea `client/src/admin/LicenciasGate.tsx` siguiendo el mockup §3 (`.lic-grid`, `.lic-card`,
`.lic-card.sel`, `.lic-reader`, `.accept`) y §5 (`.gate`, `.field`) para el caso `puerta`:

```tsx
import { useMemo, useState } from "react";
import { api, type MetaPeso } from "../lib/api";

export function LicenciasGate({ token, items, onListo }: {
  token: string; items: MetaPeso[]; onListo: () => void;
}) {
  // Agrupado por texto: la misma licencia que cubre dos pesos se lee y se
  // acepta una vez, no dos — mockup §3.
  const grupos = useMemo(() => {
    const m = new Map<string, { texto: string; para: MetaPeso[] }>();
    for (const it of items) {
      if (it.puerta) continue; // la puerta no se acepta aquí, es la sección de abajo
      const g = m.get(it.licencia) ?? { texto: it.licencia_texto, para: [] };
      g.para.push(it);
      m.set(it.licencia, g);
    }
    return [...m.entries()].map(([licencia, v]) => ({ licencia, ...v }));
  }, [items]);

  const puertas = items.filter((it) => it.puerta);
  const [sel, setSel] = useState(grupos[0]?.licencia ?? null);
  const [aceptando, setAceptando] = useState(false);
  const [tokenProveedor, setTokenProveedor] = useState("");
  const [error, setError] = useState<string | null>(null);

  async function aceptarYDescargar() {
    setAceptando(true);
    setError(null);
    try {
      const licencias: Record<string, string[]> = {};
      grupos.forEach((g) => { licencias[g.licencia] = g.para.map((p) => p.id); });
      await api.post("/v1/admin/models/accept-licenses", { licencias }, token);
      await api.post("/v1/admin/models/download", { items: grupos.flatMap((g) => g.para.map((p) => p.id)) }, token);
      onListo();
    } catch (e) {
      setError(String(e));
    } finally {
      setAceptando(false);
    }
  }

  async function guardarTokenYDescargar(item: MetaPeso) {
    setError(null);
    try {
      if (tokenProveedor) {
        await api.patch("/v1/admin/models/provider-token", { token: tokenProveedor }, token);
      }
      await api.post("/v1/admin/models/download", { items: [item.id] }, token);
      onListo();
    } catch (e) {
      setError(String(e));
    }
  }

  return (
    <div>
      {error && <p className="mb-3 text-[11px] text-danger-fg">{error}</p>}

      {grupos.length > 0 && (
        <>
          <div className="grid grid-cols-3 gap-[9px]">
            {grupos.map((g) => (
              <button key={g.licencia} onClick={() => setSel(g.licencia)}
                className={`rounded-[10px] border p-[11px_12px] text-left transition-colors duration-300 ease-expo
                  ${sel === g.licencia ? "border-white/[.34] bg-elevated" : "border-border bg-panel hover:border-white/[.22]"}`}>
                <h4 className="flex items-center gap-[7px] text-[11.5px] font-medium">
                  <span className={`h-[5px] w-[5px] shrink-0 rounded-full border border-subtle
                    ${sel === g.licencia ? "border-fg bg-fg" : ""}`} />
                  {g.licencia}
                </h4>
                <span className="mt-1 block text-[9.5px] text-subtle">
                  {g.para.map((p) => p.nombre).join(" · ")}
                </span>
              </button>
            ))}
          </div>

          {sel && (
            <div className="mt-[9px] rounded-[10px] border border-border bg-panel p-[13px_15px]">
              <h4 className="mb-2 text-[12.5px] font-medium">{sel}</h4>
              <div className="max-h-[150px] overflow-auto font-mono text-[10.5px] leading-[1.75] text-muted">
                {grupos.find((g) => g.licencia === sel)?.texto || "sin texto cacheado — revisa licencia_url"}
              </div>
            </div>
          )}

          <div className="mt-3.5 flex items-center gap-2.5 rounded-[10px] border border-white/[.16] bg-panel p-3">
            <p className="text-[11px] text-muted">
              Acepto los términos de las <b className="text-fg">{grupos.length}</b> licencias en nombre de este servidor.
            </p>
            <button disabled={aceptando} onClick={aceptarYDescargar}
              className="ml-auto rounded-lg bg-accent px-3.5 py-2 text-[11.5px] font-medium text-black disabled:opacity-40">
              {aceptando ? "aceptando…" : "Aceptar y descargar"}
            </button>
          </div>
        </>
      )}

      {puertas.map((p) => (
        <div key={p.id} className="mt-4 rounded-[10px] border border-draw/[.34] bg-draw/[.045] p-[13px_14px]">
          <h4 className="mb-1.5 text-[11.5px] font-medium">{p.nombre} no se puede descargar sin tu intervención</h4>
          <p className="max-w-[78ch] text-[10.5px] leading-[1.7] text-muted">
            Su licencia obliga a aceptarla en el sitio del proveedor y a usar un token propio.
            Lumi no acepta términos en tu nombre.
          </p>
          <div className="mt-[11px] flex items-center gap-2">
            <input type="password" value={tokenProveedor} onChange={(e) => setTokenProveedor(e.target.value)}
              placeholder="token del proveedor · se guarda y se redacta en los logs"
              className="min-w-[220px] flex-1 rounded-lg border border-border bg-elevated px-2.5 py-1.5 font-mono text-[10.5px] text-fg outline-none focus:border-white/40" />
            <button onClick={() => guardarTokenYDescargar(p)}
              className="rounded-lg border border-white/15 px-3 py-1.5 text-[11px] text-fg">
              Guardar y descargar
            </button>
          </div>
        </div>
      ))}
    </div>
  );
}
```

Añade `MetaPeso` a `client/src/lib/api.ts` (mismo shape que el Rust `MetaPeso` de Step 1).

- [ ] **Step 3: Conectar el botón "Completar nivel" de `ModelosView`**

En `client/src/admin/ModelosView.tsx`, sustituye el `onClick` marcado con el comentario "Tarea 10
conecta esto" por abrir `LicenciasGate` con los metadatos de `n.resolucion.faltan`:

```tsx
import { LicenciasGate } from "./LicenciasGate";
// ...
const [completando, setCompletando] = useState<{ nivel: string; items: import("../lib/api").MetaPeso[] } | null>(null);
const [metadatos, setMetadatos] = useState<import("../lib/api").MetaPeso[]>([]);

useEffect(() => {
  api.get<import("../lib/api").MetaPeso[]>("/v1/admin/models/metadata", token).then(setMetadatos).catch(() => {});
}, [token]);

// dentro del onClick del botón "Completar {n.nombre}":
onClick={(e) => {
  e.stopPropagation();
  const pendientes = metadatos.filter((m) => n.resolucion.faltan.includes(m.id));
  setCompletando({ nivel: n.id, items: pendientes });
}}
```

Y, tras el `.map` de niveles, renderiza el gate cuando `completando` no es `null`:
```tsx
{completando && (
  <div className="mt-4">
    <LicenciasGate token={token} items={completando.items}
      onListo={() => { setCompletando(null); api.get<NivelEstado[]>("/v1/admin/models", token).then(setNiveles); }} />
  </div>
)}
```

- [ ] **Step 4: La fila del proveedor en `KeysView`**

En `client/src/admin/KeysView.tsx`, sustituye el cartel estático "Proveedor de pesos" por uno que
lea `GET /v1/admin/models/provider-token` de verdad y permita guardarlo aquí también (no solo
desde la puerta):

```tsx
import { useEffect, useState } from "react";
import { api, type ProviderTokenState } from "../lib/api";
// ...
export function KeysView({ token }: { token: string }) {
  const [estado, setEstado] = useState<ProviderTokenState | null>(null);
  const [valor, setValor] = useState("");

  useEffect(() => {
    api.get<ProviderTokenState>("/v1/admin/models/provider-token", token).then(setEstado).catch(() => {});
  }, [token]);

  async function guardar() {
    const r = await api.patch<ProviderTokenState>("/v1/admin/models/provider-token", { token: valor }, token);
    setEstado(r);
    setValor("");
  }

  return (
    <Seccion titulo="API Keys" grupo="Servidor">
      <p className="text-[11px] leading-[1.72] text-subtle">
        Ninguna se muestra entera, ni después de guardarla.
      </p>
      <div className="mt-4"><MapRow token={token} /></div>

      <div className="mt-3 flex items-center gap-3 rounded-[11px] border border-border p-[11px_14px]">
        <span className="min-w-0 text-[11.5px] text-muted">
          Proveedor de pesos
          <small className="ml-2 text-[9.5px] text-subtle">para modelos tras la puerta de su proveedor</small>
        </span>
        <input type="password" value={valor} onChange={(e) => setValor(e.target.value)}
          placeholder={estado?.has_token ? "token guardado · escribe para sustituirlo" : "token del proveedor"}
          className="ml-auto min-w-[180px] rounded-lg border border-border bg-elevated px-2.5 py-1 font-mono text-[10.5px] text-fg outline-none focus:border-white/40" />
        <button onClick={guardar} className="rounded-lg border border-white/15 px-2.5 py-1 text-[10.5px] text-fg">
          Guardar
        </button>
      </div>

      <p className="mt-4 text-[11px] leading-[1.72] text-subtle">
        Las de los orígenes de red —Mapillary, Flickr, Google— viven en el Lumi Indexer.
      </p>
    </Seccion>
  );
}
```

- [ ] **Step 5: Comprobar**

Run: `cd client && npm run lint && npm run build`
Expected: verde.

- [ ] **Step 6: Commit**

```bash
git add client/src/admin/ModelosView.tsx client/src/admin/LicenciasGate.tsx client/src/admin/KeysView.tsx client/src/lib/api.ts crates/lumid/src/routes/models.rs crates/lumid/src/main.rs
git commit -F <(cat <<'EOF'
Las licencias se eligen para leerse, y la puerta deja de ser un cartel

LicenciasGate agrupa por texto (una licencia, un gesto), con la rejilla
seleccionable del mockup en vez de tres cuerpos cortados a la vez. La
puerta del proveedor gana su propio bloque con token opcional. KeysView
lee y guarda el token de verdad en vez del cartel estatico que dejo el
esqueleto del panel.
EOF
)
```

---

### Task 11: Los toasts — progreso y licencias pendientes, en cualquier sección

**Files:**
- Create: `client/src/admin/ModelToasts.tsx`
- Modify: `client/src/admin/AdminPanel.tsx`
- Modify: `client/src/admin/ModelosView.tsx`

**Interfaces:**
- Consume: `GET /v1/admin/model-task` (Tarea 7), `Seccion` (tipo, de `Sidebar.tsx`).
- Produce: `ModelToasts({ token, onIr }: { token: string; onIr: (s: Seccion) => void })`, montado
  una vez en `AdminPanel` — no dentro de `ModelosView`, que es justo el punto de la Tarea 8 de la
  spec (§8: el toast no vive en la sección Modelos).

- [ ] **Step 1: El componente**

Crea `client/src/admin/ModelToasts.tsx` con el aspecto del mockup §6 (`.toasts`, `.toast`,
`.toast.warn`):

```tsx
import { useEffect, useState } from "react";
import { api, type TareaModelo } from "../lib/api";
import type { Seccion } from "./Sidebar";

export function ModelToasts({ token, onIr, licenciasPendientes }: {
  token: string;
  onIr: (s: Seccion) => void;
  /** Estado de ESTA sesión del panel (Tarea 10 lo levanta): no hay tarea
   *  todavía mientras la pantalla de licencias sigue abierta, así que no
   *  hay nada que el servidor pueda descubrir sobre esto — spec §8. */
  licenciasPendientes: boolean;
}) {
  const [tarea, setTarea] = useState<TareaModelo | null>(null);

  useEffect(() => {
    let vivo = true;
    async function sondear() {
      try {
        const t = await api.get<TareaModelo | null>("/v1/admin/model-task", token);
        if (vivo) setTarea(t);
      } catch { /* red caída un instante: se reintenta en el próximo tick */ }
    }
    void sondear();
    const i = setInterval(sondear, 3000);
    return () => { vivo = false; clearInterval(i); };
  }, [token]);

  if (!tarea && !licenciasPendientes) return null;

  return (
    <div className="pointer-events-none fixed bottom-4 right-4 z-50 flex flex-col gap-2.5" style={{ width: 308 }}>
      {tarea && (
        <button onClick={() => onIr("modelos")}
          className="jg-press pointer-events-auto flex items-start gap-2.5 rounded-[11px] border border-white/[.14]
            bg-[rgba(20,22,26,.97)] p-[11px_12px] text-left shadow-lg shadow-black/40 backdrop-blur-xl"
          style={{ animation: "jg-fade-rise .5s cubic-bezier(.16,1,.3,1) both" }}>
          <div className="min-w-0 flex-1">
            <div className="text-[11px] text-fg">Descargando modelos</div>
            <div className="mt-0.5 font-mono text-[9.5px] text-subtle">
              {tarea.item_actual ?? "…"}{tarea.pct != null ? ` · ${tarea.pct}%` : ""}
            </div>
            {tarea.pct != null && (
              <div className="mt-[7px] h-[3px] overflow-hidden rounded-sm bg-elevated">
                <div className="h-full bg-fg transition-[width] duration-500 ease-expo" style={{ width: `${tarea.pct}%` }} />
              </div>
            )}
            <div className="mt-2 text-[9.5px] text-draw-fg">ver progreso →</div>
          </div>
        </button>
      )}
      {licenciasPendientes && (
        <button onClick={() => onIr("modelos")}
          className="jg-press pointer-events-auto flex items-start gap-2.5 rounded-[11px] border border-white/[.14]
            bg-[rgba(20,22,26,.97)] p-[11px_12px] text-left shadow-lg shadow-black/40 backdrop-blur-xl"
          style={{ animation: "jg-fade-rise .5s cubic-bezier(.16,1,.3,1) both" }}>
          <div className="min-w-0 flex-1">
            <div className="text-[11px] text-fg">Hay licencias por aceptar</div>
            <div className="mt-0.5 text-[9.5px] text-subtle">La descarga no sigue sin esto.</div>
            <div className="mt-2 text-[9.5px] text-draw-fg">revisar licencias →</div>
          </div>
        </button>
      )}
    </div>
  );
}
```

Añade `TareaModelo` a `client/src/lib/api.ts`:
```ts
export interface TareaModelo {
  id: string;
  item_actual: string | null;
  pct: number | null;
}
```

- [ ] **Step 2: `licenciasPendientes` sube de `ModelosView` a `AdminPanel`**

`ModelosView` levanta un booleano cuando `completando` (Tarea 10, Step 3) no es `null` y no se ha
resuelto. En `client/src/admin/ModelosView.tsx`, añade una prop opcional:

```tsx
export function ModelosView({ token, nivelInicial, onLicenciasPendientesChange }: {
  token: string; nivelInicial?: string; onLicenciasPendientesChange?: (p: boolean) => void;
}) {
  // ...
  useEffect(() => {
    onLicenciasPendientesChange?.(completando !== null);
  }, [completando, onLicenciasPendientesChange]);
```

- [ ] **Step 3: Montar en `AdminPanel`**

En `client/src/admin/AdminPanel.tsx`:
```tsx
import { ModelToasts } from "./ModelToasts";
// ...
const [licenciasPendientes, setLicenciasPendientes] = useState(false);
// ...
: seccion === "modelos" ? <ModelosView token={token} onLicenciasPendientesChange={setLicenciasPendientes} />
```

Y al final del JSX del panel (hermano del `<Sidebar>`/contenido, no dentro de la sección):
```tsx
<ModelToasts token={token} onIr={setSeccion} licenciasPendientes={licenciasPendientes} />
```

- [ ] **Step 4: Comprobar**

Run: `cd client && npm run lint && npm run build`
Expected: verde. Verificación manual (no hay daemon corriendo en Windows per `CLAUDE.md`): confirma
visualmente con `npm run dev` que el toast no aparece cuando `tarea` es `null` y
`licenciasPendientes` es `false` — no debe haber una caja vacía flotando.

- [ ] **Step 5: Commit**

```bash
git add client/src/admin/ModelToasts.tsx client/src/admin/AdminPanel.tsx client/src/admin/ModelosView.tsx client/src/lib/api.ts
git commit -F <(cat <<'EOF'
El progreso de una descarga no depende de estar en Modelos

ModelToasts se monta en AdminPanel, no en ModelosView: sondea
/v1/admin/model-task cada tres segundos sin importar que seccion este
abierta, y un clic lleva a Modelos. El toast de licencias pendientes es
estado de esta sesion del panel, no del servidor -- no hay tarea todavia
mientras la pantalla de aceptar sigue sin resolverse.
EOF
)
```

---

### Task 12: El paso del asistente

**Files:**
- Create: `client/src/wizard/ModelosStep.tsx`
- Modify: `client/src/App.tsx`

**Interfaces:**
- Consume: `ModelosView` (Tarea 9/10).
- Produce: nada que otra tarea use — es el último consumidor.

- [ ] **Step 1: El paso**

Crea `client/src/wizard/ModelosStep.tsx` — envuelve `ModelosView` y decide cuándo "Mini" está
completo para poder avanzar, per spec §11 ("completar Mini basta para avanzar"):

```tsx
import { useEffect, useState } from "react";
import { api, type NivelEstado } from "../lib/api";
import { ModelosView } from "../admin/ModelosView";

export function ModelosStep({ token, onStatusChange }: {
  token: string; onStatusChange?: (listo: boolean) => void;
}) {
  const [niveles, setNiveles] = useState<NivelEstado[]>([]);

  async function refrescar() {
    const n = await api.get<NivelEstado[]>("/v1/admin/models", token);
    setNiveles(n);
  }
  useEffect(() => { void refrescar(); }, [token]);

  useEffect(() => {
    const mini = niveles.find((n) => n.id === "mini");
    onStatusChange?.(!!mini && mini.resolucion.faltan.length === 0);
  }, [niveles, onStatusChange]);

  return (
    <div>
      <p className="mb-3 max-w-[52ch] text-[11px] text-muted">
        Con Mini basta para seguir — Pro y Vision se completan cuando quieras, desde el panel.
      </p>
      <ModelosView token={token} nivelInicial="mini" />
    </div>
  );
}
```

- [ ] **Step 2: Insertarlo en el asistente**

En `client/src/App.tsx`, dentro del bloque `mode === "wizard"`, cambia el manejo especial de
`step === 2` para que deje de saltar directo a `setMode(...)` y en su lugar avance a un nuevo
`step === 3`:

```tsx
onNext={() => {
  if (step === 1) { document.getElementById("admin-submit")?.click(); return; }
  if (step === 3) { setMode(useServer.getState().isAdmin ? "admin" : "picker"); return; }
  setStep((s) => s + 1);
}}
nextDisabled={
  (step === 0 && (!hello || hello.state !== "unclaimed")) ||
  (step === 2 && !runtimeDone) ||
  (step === 3 && !modelosDone)
}
nextLabel={step === 3 ? "Terminar" : "Siguiente"}
nextBusy={step === 1 && adminBusy}>
{step === 0 && <PairStep onDone={() => setStep(1)} />}
{step === 1 && <AdminStep bootstrapToken={bootstrapToken} onDone={() => setStep(2)} onBusyChange={setAdminBusy} />}
{step === 2 && <ProvisionStep onDone={() => setStep(3)} onStatusChange={setRuntimeDone} />}
{step === 3 && <ModelosStep token={useServer.getState().token!} onStatusChange={setModelosDone} />}
```

`ProvisionStep`'s `onDone` cambia de `() => setMode("picker")` a `() => setStep(3)` — antes era el
último paso construido y saltaba directo a la app; ahora hay uno más después. Añade el estado
lifted junto a `runtimeDone`:
```tsx
const [modelosDone, setModelosDone] = useState(false);
```

- [ ] **Step 3: Comprobar**

Run: `cd client && npm run lint && npm run build`
Expected: verde.

- [ ] **Step 4: Commit**

```bash
git add client/src/wizard/ModelosStep.tsx client/src/App.tsx
git commit -F <(cat <<'EOF'
El asistente pide Mini antes de soltarte en la app

Un paso nuevo despues de Runtime reutiliza ModelosView entero -- misma
pantalla que el panel, spec 11. Completar Mini basta para avanzar; Pro y
Vision quedan para el panel cuando quieras. ProvisionStep ya no salta
directo a la app: ahora hay un paso mas detras.
EOF
)
```

---

### Task 13: Cerrar documentos

**Files:**
- Modify: `ARCHITECTURE.md`
- Modify: `CLAUDE.md`
- Modify: `FUTURO.md`

**Interfaces:**
- Consume: nada.
- Produce: nada.

- [ ] **Step 1: `ARCHITECTURE.md`**

Busca la fila de la tabla del subsistema 3 (la que la Tarea 9 del plan del esqueleto ya dividió en
cuatro filas: skeleton/3a/3b/3c). Cambia el estado de **3a** de "con spec pendiente" a
"Terminado".

- [ ] **Step 2: `CLAUDE.md`**

Busca la frase (mencionada en la spec §16) donde `registros/` se describe como editado a mano —
probablemente en la sección de convenciones o en la descripción de `registros/`. Ajusta para
reflejar que modelos/verificadores/motores/geo ahora se instalan desde el panel o el asistente, y
que editar a mano sigue siendo posible pero ya no es la única vía.

- [ ] **Step 3: `FUTURO.md`**

Quita la entrada "rotar la clave del proveedor de mapas" si sigue estando bajo el 3b (ya se
resolvió en el ciclo del esqueleto — confirma que de verdad ya no está antes de tocar nada). Añade
una entrada nueva: "gestión de versiones de un mismo modelo" (spec §2, fuera de alcance de este
ciclo — que convivan dos versiones de MegaLoc es un problema de Qdrant y de las capas de índice).

- [ ] **Step 4: Commit**

```bash
git add ARCHITECTURE.md CLAUDE.md FUTURO.md
git commit -F <(cat <<'EOF'
3a pasa a terminado, y los registros dejan de ser solo edicion a mano

ARCHITECTURE.md marca 3a hecho; CLAUDE.md deja de describir registros/
como edicion manual exclusiva; FUTURO.md gana la gestion de versiones de
un mismo modelo, fuera de alcance de este ciclo.
EOF
)
```

---

## Self-Review

**Cobertura de la spec** (`2026-08-13-modelos-3a-design.md`):
- §3 (cuatro campos, cuatro registros + motores) → Tarea 1.
- §4 (hash conocido vs. solo contra sí mismo, borrar si no cuadra) → Tarea 4 (worker) y Tarea 10
  (interfaz ya lo mostraba en el mockup — el color/texto exacto está en el HTML aprobado, no se
  reinventa en código React nuevo más allá de leer `faltan`).
- §5 (aceptar antes de tocar la red) → Tareas 2 y 10.
- §6 (puerta) → Tareas 8 y 10.
- §7 (tarea del servidor + progreso) → Tareas 4, 5.
- §8 (toasts, descubrimiento por servidor) → Tareas 7, 11.
- §9 (apagado) → Tarea 9 (dot styling en `ModelosView`, siguiendo el mockup).
- §10 (recarga en caliente) → Tareas 3, 6.
- §11 (dónde vive) → Tareas 9-10 (panel) y 12 (asistente).
- §12 (`model_task_id` en meta) → Tarea 5.
- §13 (interfaz aprobada) → todo el bloque de cliente.
- §14 (pruebas de lógica pura) → Tarea 1 (motor sin id) y Tarea 7 (resolver sin contar doble).
- §16 (documentos) → Tarea 13.

**Huecos encontrados y resueltos durante la escritura:**
- El registro de "motores" no existía — resuelto con el usuario (Tarea 1 lo crea desde cero,
  incluyendo el caso especial de PaddleOCR).
- `cargar_modelos`/`cargar_verificadores` estaban escritos pero nunca llamados — Tarea 3 los
  conecta por primera vez, no solo "recarga" algo que ya cargaba.
- `geo` no tenía manifiesto de licencias — Tarea 1 añade `registros/geo/registro.json`.
- El token de proveedor no tenía precedente de almacenamiento — resuelto siguiendo el patrón real
  y ya existente de `map_key` (plaintext en `meta`, nunca devuelto), no inventando cifrado nuevo.

**Consistencia de tipos:** `TareaModelo`/`MetaPeso`/`ItemDescarga`/`NivelEstado`/`Resolucion` se
definen una vez (Tareas 5, 7, 10) y se reutilizan con el mismo nombre en las tareas siguientes —
verificado al escribir cada tarea posterior contra la anterior.

---

Plan completo y guardado en `docs/superpowers/plans/2026-08-14-modelos-3a.md`. Dos formas de
ejecutarlo:

1. **Subagent-Driven (recomendado)** — un subagente fresco por tarea, revisión entre tareas.
2. **Ejecución en esta sesión** — por lotes, con checkpoints.

¿Cuál prefieres?
